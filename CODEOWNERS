# CODEOWNERS — required reviewers per path.
# `main` is protected: PRs need a passing CI run and approval from the owners below.
#
# Single owner for now (@jberry-auto). As the project grows, split these into
# GitHub teams (e.g. content vs. infra owners). CODEOWNERS resolves @handles and
# team slugs; a bare email only works if it is a verified email on a GitHub
# account with repo access — the contact address for the project is jacob@autopwn.sh.

# Default — everything requires the owner's review unless a more specific rule matches.
*                       @jberry-auto

# Taxonomy content.
/content/               @jberry-auto

# Documentation / specs.
/docs/                  @jberry-auto

# Infrastructure, CI/CD, and security-sensitive config.
/.github/               @jberry-auto
/.do/                   @jberry-auto
/Dockerfile             @jberry-auto
/scripts/               @jberry-auto

# Governance files.
/CLAUDE.md              @jberry-auto
/CODEOWNERS             @jberry-auto
/CONTRIBUTING.md        @jberry-auto
/LICENSE                @jberry-auto
